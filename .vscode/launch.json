{
    "version": "0.2.0",
    "configurations": [
        {
            "type": "node-terminal",
            "name": "dev",
            "request": "launch",
            "command": "npm run dev",
            "cwd": "${workspaceFolder}/back-pokedex"
        }
    ]
}